# -- perform (g)it (ch)eckout using fzf
alias gch='fzf_git_checkout'
# -- perform (g)it (sh)ow using fzf
alias gsh='fzf_git_show_commit'
# -- perform (g)it (sh)ow incl. (a)uthor using fzf
alias gsha='fzf_git_show_commit_with_author'
